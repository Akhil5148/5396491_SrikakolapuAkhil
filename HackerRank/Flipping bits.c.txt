#include <stdio.h>
#include <stdint.h>

uint32_t flippingBits(uint32_t n) {
    return ~n;
}

int main() {
    int q;
    scanf("%d", &q);
    for (int i = 0; i < q; i++) {
        uint32_t n;
        scanf("%u", &n);
        printf("%u\n", flippingBits(n));
    }
    return 0;
}

