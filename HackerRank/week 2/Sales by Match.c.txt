#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);
    int ar[101] = {0}, color;
    int pairs = 0;
    for (int i = 0; i < n; i++) {
        scanf("%d", &color);
        ar[color]++;
        if (ar[color] % 2 == 0) pairs++;
    }
    printf("%d", pairs);
    return 0;
}
